#include <iostream>

using namespace std;

int main(){
	int x = 2; // x is a variable 2 is a literal.
	int y = (x > 0) ? 1 : 2; //true left, false right

	cout << x + 3 << endl; // + / - * etc..
	cout << "y : " << y << endl;

	cout << "My Home" << endl;

	return 0;
}
